<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidRoom" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Custom" parent="Theme.Material3.Light">
        <item name="toolbarStyle">@style/Theme.Custom.Toolbar</item>
        <item name="android:statusBarColor">@color/selected</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:titleTextColor">@color/white</item>

        <!--        <item name="actionOverflowButtonStyle">@style/MyOverflowButtonStyle</item>-->
        <!--        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>-->
    </style>

    <style name="Theme.Custom.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/selected</item>
    </style>

    <style name="Theme.AndroidRoom" parent="Base.Theme.AndroidRoom" />
</resources>